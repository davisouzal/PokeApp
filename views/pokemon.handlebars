<!DOCTYPE html>
<html lang="pt">
    <head>
        <meta charset="UTF-8">
        <meta http-equiv="X-UA-Compatible" content="IE=edge">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>{{pokemon.name}} - Pokemon info</title>
    </head>
    <body>
        <h1 style="text-transform: capitalize;">{{pokemon.name}}</h1>
        <img src="{{pokemon.sprites.front_default}}" alt="{{pokemon.name}}">
        <h2>Altura: {{pokemon.height}}</h2>
        <h2>Peso: {{pokemon.weight}}</h2>
        <p>
            Tipos: {{#each pokemon.types}}{{this.type.name}}{{#unless @last}}, {{/unless}}{{/each}}
        </p>
    </body>
</html>